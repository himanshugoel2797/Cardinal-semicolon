CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

FILE(GLOB LIBC_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/src/*.c)
ADD_LIBRARY(c STATIC "${LIBC_SRCS}")
TARGET_INCLUDE_DIRECTORIES(c PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/inc)
TARGET_INCLUDE_DIRECTORIES(c SYSTEM PUBLIC "${KERN_STDLIB_INCLUDE_DIR}")