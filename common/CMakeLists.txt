CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

FILE(GLOB LIBC_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/src/*.c)
ADD_LIBRARY(c_kern STATIC "${LIBC_SRCS}")
TARGET_INCLUDE_DIRECTORIES(c_kern PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/inc)
TARGET_INCLUDE_DIRECTORIES(c_kern SYSTEM PUBLIC "${KERN_STDLIB_INCLUDE_DIR}")