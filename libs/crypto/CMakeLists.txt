CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

FILE(GLOB CRYPTO_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/*.c)

ADD_LIBRARY(crypto STATIC ${CRYPTO_SRCS})
TARGET_INCLUDE_DIRECTORIES(crypto SYSTEM PUBLIC "${KERN_STDLIB_INCLUDE_DIR}")
TARGET_LINK_LIBRARIES(crypto)