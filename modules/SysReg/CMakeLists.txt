CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

FILE(GLOB SYSREG_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/src/*.c)

ADD_EXECUTABLE(SysReg.elf "${SYSREG_SRCS}")

ADD_CUSTOM_TARGET(SysReg.celf 
    DEPENDS SysReg.elf
    COMMAND ${CELF_GEN} SysReg.elf -o SysReg.celf
)